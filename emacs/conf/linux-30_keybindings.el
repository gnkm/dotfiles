;;; linux-30_keybindings.el --- keybindings for linux

;;; Commentary:
;; keybindings for linux

(use-package skk
  :config
  (global-set-key "\C-x\C-j" 'skk-mode))

;;; linux-30_keybindings.el ends here
